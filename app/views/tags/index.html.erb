<h1>All Tags</h1>

<ul id="tags">
    <% @tag.each do |taggy| %>
        <li>
            <%= link_to taggy.name, tag_path(taggy) %>
        </li>
    <% end %>
</ul>

<%= link_to "<<Back to Articles List", articles_path%>